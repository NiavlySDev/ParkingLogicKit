 [Utilisateur ouvre l'application]
            │
            ▼
      [Page de login]
            │
            │ --- Login réussi ---> Backend renvoie un TOKEN signé
            │      { username, role, exp }
            │
            ▼
   [AuthService.setToken(token)]
            │
            │ Stocke le token dans localStorage
            │ Démarre le timeout de session
            ▼
[Navigation vers une page protégée]
            │
            │
      [AuthGuard]
<<<<<<< Updated upstream
      ┌────────────────────┐
      │ isLoggedIn() ?     │
      │ (token présent &   │
      │  non expiré)       │
      └─────────┬──────────┘
=======
      ┌───────────────────┐
      │ isLoggedIn() ?    │
      │ (token présent &  │
      │  non expiré)      │
      └─────────┬─────────┘
>>>>>>> Stashed changes
                │ Oui
                ▼
            [RoleGuard]
      ┌─────────────────────────┐
      │ getRole() depuis token  │
      │ Vérifie data.role de la │
      │ route (ex: 'Admin')     │
      └─────────┬───────────────┘
                │ Oui
                ▼
          [Page affichée]
                │
                │ Utilisateur interagit
                │ → resetTimeout() est appelé
                ▼
          [Logout automatique après 15 min]
                │
                ▼
      localStorage.removeItem('authToken')
      redirection vers /sign-in